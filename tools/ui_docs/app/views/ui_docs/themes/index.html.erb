<%# Copied from RapidUI v0.1.4 | Source: rapid_ui/docs/app/views/themes/index.html.erb %>
<% ui.layout.head.title = ["Themes"] %>

<div class="max-w-4xl mx-auto">
  <div class="text-center">
    <h1 class="typography-h1 text-4xl mb-4">Themes</h1>
    <p class="typography-body text-xl text-gray-600 max-w-2xl mx-auto">
      Color schemes and styling presets for consistent UI design.
    </p>
  </div>

  <%= render_feature_list @available_themes.map { |theme|
    { name: theme.title + " →", description: theme.description, path: theme_path(theme.path) }
  } %>
</div>
